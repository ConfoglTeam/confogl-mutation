# https://docs.travis-ci.com/user/job-lifecycle

language: minimal

dist: focal

git:
  depth: 5

before_install:
- sudo apt-get -y install squirrel3

stages:
  - test

jobs:
  include:
    - stage: test
      script:
        - squirrel test.nut
